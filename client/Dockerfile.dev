FROM node:22-slim

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy source code (this will be overridden by volume mount)
COPY . .

EXPOSE 5173

# Development mode with Vite dev server (hot reloading)
CMD ["npm", "run", "dev"]